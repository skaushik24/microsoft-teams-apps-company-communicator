name: Build and deploy TechUnicorn App

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: 🧾 Checkout code
        uses: actions/checkout@v3

      - name: 🛠️ Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '6.0.x'

      - name: 🧱 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: ⚛️ Build React frontend
        working-directory: Source/CompanyCommunicator/ClientApp
        run: |
          npm install
          npm run build

      - name: 📁 Clean + copy React build to wwwroot
        run: |
          $wwwrootPath = "Source/CompanyCommunicator/wwwroot"
          if (Test-Path $wwwrootPath) {
            Remove-Item -Recurse -Force $wwwrootPath
          }
          New-Item -ItemType Directory -Path $wwwrootPath
          Copy-Item -Path Source/CompanyCommunicator/ClientApp/build/* -Destination $wwwrootPath -Recurse -Force

      - name: 🔨 Publish .NET project
        working-directory: Source/CompanyCommunicator
        run: dotnet publish Microsoft.Teams.Apps.CompanyCommunicator.csproj -c Release -o ./output

      - name: 📁 Ensure React build is in publish output
        run: |
          $sourceWwwroot = "Source/CompanyCommunicator/wwwroot"
          $destinationWwwroot = "Source/CompanyCommunicator/output/wwwroot"
          if (-not (Test-Path $destinationWwwroot)) {
            New-Item -ItemType Directory -Path $destinationWwwroot
          }
          Remove-Item -Recurse -Force "$destinationWwwroot\*" -ErrorAction SilentlyContinue
          Copy-Item -Path "$sourceWwwroot\*" -Destination $destinationWwwroot -Recurse -Force

      - name: 📦 Zip publish output (flattened for Azure)
        run: Compress-Archive -Path Source/CompanyCommunicator/output/* -DestinationPath publish.zip

      - name: ✅ Confirm ZIP contents (debug)
        run: |
          Expand-Archive -Path publish.zip -DestinationPath tempZipCheck
          Get-ChildItem -Path tempZipCheck -Recurse

      - name: 🚀 Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: TechUnicorn-Communicator
          package: publish.zip
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE }}
